export default {
  default: {
    workspace: 'your local project clone path [if not config will use tmp path every time]',
    deployTo: '/server-project-path',
    repositoryUrl: 'git://...',
    keepReleases: 8,
    only: null,
    ignores: [],
    shared: {
      dirs: ['node_modules'],
      files: ['pm2.config.json']
    },
    staging: {
      servers: [{
        host: '',
        user: ''
      }],
      branch: 'develop'
    }
  }
}
