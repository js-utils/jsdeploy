{
  "default": {
    "workspace": "your local project clone path [if not config will use tmp path every time]",
    "deployTo": "/mnt/www/test-project",
    "repositoryUrl": "git://...",
    "keepReleases": 8,
    "only": []
  },
  "staging": {
    "servers": [{
      "host": "39.104.121.122",
      "username": "deploy"
    }],
    "branch": "develop"
  }
}
